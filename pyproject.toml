[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tunnex_2"
version = "0.6.0"

[project.scripts]
tunnex_2 = "tunnex_2.main:tunnex_2_cli"

[tool.setuptools.packages.find]
where = ["."]